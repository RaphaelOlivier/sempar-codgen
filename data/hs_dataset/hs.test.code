class Archmage ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Archmage " , 6 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 7 , spell_damage = 1 )
class Booty #MERGE# Bay #MERGE# Bodyguard ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Booty Bay Bodyguard " , 5 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 5 , 4 , taunt = True )
class Darkscale #MERGE# Healer ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Darkscale Healer " , 5 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , battlecry = Battlecry ( Heal ( 2 ) , Character #MERGE# Selector ( ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 5 )
class Fiery #MERGE# War #MERGE# Axe ( Weapon #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Fiery War Axe " , 2 , CHARACTER_CLASS . WARRIOR , CARD_RARITY . FREE ) #NEWLINE# #NEWLINE# #INDENT# def create_weapon ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Weapon ( 3 , 2 )
class Frostwolf #MERGE# Warlord ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Frostwolf Warlord " , 5 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , battlecry = Battlecry ( Give ( [ Buff ( Change #MERGE# Attack ( Count ( Minion #MERGE# Selector ( ) ) ) ) , Buff ( Change #MERGE# Health ( Count ( Minion #MERGE# Selector ( ) ) ) ) ] ) , Self #MERGE# Selector ( ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 4 )
class Hellfire ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Hellfire " , 4 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . FREE ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# targets = copy . copy ( game . other_player . minions ) #NEWLINE# #INDENT# #INDENT# targets . extend ( game . current_player . minions ) #NEWLINE# #INDENT# #INDENT# targets . append ( game . other_player . hero ) #NEWLINE# #INDENT# #INDENT# targets . append ( game . current_player . hero ) #NEWLINE# #INDENT# #INDENT# for minion in targets : #NEWLINE# #INDENT# #INDENT# #INDENT# minion . damage ( player . effective_spell_damage ( 3 ) , self )
class Innervate ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Innervate " , 0 , CHARACTER_CLASS . DRUID , CARD_RARITY . FREE ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# if player . mana < 8 : #NEWLINE# #INDENT# #INDENT# #INDENT# player . mana += 2 #NEWLINE# #INDENT# #INDENT# else : #NEWLINE# #INDENT# #INDENT# #INDENT# player . mana = 10
class Magma #MERGE# Rager ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Magma Rager " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . FREE ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 5 , 1 )
class Mortal #MERGE# Coil ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Mortal Coil " , 1 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . COMMON , target_func = hearthbreaker . targeting . find_minion_spell_target ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# if self . target . health <= player . effective_spell_damage ( 1 ) and not self . target . divine_shield : #NEWLINE# #INDENT# #INDENT# #INDENT# self . target . damage ( player . effective_spell_damage ( 1 ) , self ) #NEWLINE# #INDENT# #INDENT# #INDENT# player . draw ( ) #NEWLINE# #INDENT# #INDENT# else : #NEWLINE# #INDENT# #INDENT# #INDENT# self . target . damage ( player . effective_spell_damage ( 1 ) , self )
class Polymorph ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Polymorph " , 4 , CHARACTER_CLASS . MAGE , CARD_RARITY . FREE , target_func = hearthbreaker . targeting . find_minion_spell_target ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# from hearthbreaker . cards . minions . mage import Sheep #NEWLINE# #INDENT# #INDENT# sheep = Sheep ( ) #NEWLINE# #INDENT# #INDENT# minion = sheep . create_minion ( None ) #NEWLINE# #INDENT# #INDENT# minion . card = sheep #NEWLINE# #INDENT# #INDENT# self . target . replace ( minion )
class Searing #MERGE# Totem ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Searing Totem " , 1 , CHARACTER_CLASS . SHAMAN , CARD_RARITY . FREE , False , MINION_TYPE . TOTEM ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 1 , 1 )
class Silverback #MERGE# Patriarch ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Silverback Patriarch " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , minion_type = MINION_TYPE . BEAST ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 1 , 4 , taunt = True )
class Stormwind #MERGE# Knight ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Stormwind Knight " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 5 , charge = True )
class Voodoo #MERGE# Doctor ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Voodoo Doctor " , 1 , CHARACTER_CLASS . ALL , CARD_RARITY . FREE , battlecry = Battlecry ( Heal ( 2 ) , Character #MERGE# Selector ( players = Both #MERGE# Player ( ) , picker = User #MERGE# Picker ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 1 )
class Wrath #MERGE# Of #MERGE# Air #MERGE# Totem ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Wrath of Air Totem " , 1 , CHARACTER_CLASS . SHAMAN , CARD_RARITY . FREE , False , MINION_TYPE . TOTEM ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 0 , 2 , spell_damage = 1 )
class Astral #MERGE# Communion ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Astral Communion " , 4 , CHARACTER_CLASS . DRUID , CARD_RARITY . EPIC ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# for card in player . hand : #NEWLINE# #INDENT# #INDENT# #INDENT# card . unattach ( ) #NEWLINE# #INDENT# #INDENT# #INDENT# player . trigger ( " card_discarded " , card ) #NEWLINE# #INDENT# #INDENT# player . hand = [ ] #NEWLINE# #INDENT# #INDENT# player . max_mana = 10 #NEWLINE# #INDENT# #INDENT# player . mana = 10
class Annoyo #MERGE# Tron ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Annoy-o-Tron " , 2 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , minion_type = MINION_TYPE . MECH ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 1 , 2 , divine_shield = True , taunt = True )
class Burly #MERGE# Rockjaw #MERGE# Trogg ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Burly Rockjaw Trogg " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 5 , effects = [ Effect ( Spell #MERGE# Cast ( player = Enemy #MERGE# Player ( ) ) , Action #MERGE# Tag ( Give ( Change #MERGE# Attack ( 2 ) ) , Self #MERGE# Selector ( ) ) ) ] )
class Cogmasters #MERGE# Wrench ( Weapon #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Cogmaster's Wrench " , 3 , CHARACTER_CLASS . ROGUE , CARD_RARITY . EPIC ) #NEWLINE# #NEWLINE# #INDENT# def create_weapon ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Weapon ( 1 , 3 , buffs = [ Buff ( Change #MERGE# Attack ( 2 ) , Greater #MERGE# Than ( Count ( Minion #MERGE# Selector ( Is #MERGE# Type ( MINION_TYPE . MECH ) ) ) , value = 0 ) ) ] )
class Echo #MERGE# Of #MERGE# Medivh ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Echo of Medivh " , 4 , CHARACTER_CLASS . MAGE , CARD_RARITY . EPIC ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# for minion in sorted ( copy . copy ( player . minions ) , key = lambda minion : minion . born ) : #NEWLINE# #INDENT# #INDENT# #INDENT# if len ( player . hand ) < 10 : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# player . hand . append ( minion . card )
class Floating #MERGE# Watcher ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Floating Watcher " , 5 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . COMMON , minion_type = MINION_TYPE . DEMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 4 , effects = [ Effect ( Character #MERGE# Damaged ( And ( Is #MERGE# Hero ( ) , Owners #MERGE# Turn ( ) ) ) , Action #MERGE# Tag ( Give ( [ Buff ( Change #MERGE# Attack ( 2 ) ) , Buff ( Change #MERGE# Health ( 2 ) ) ] ) , Self #MERGE# Selector ( ) ) ) ] )
class Gnomish #MERGE# Experimenter ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Gnomish Experimenter " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE , battlecry = ( Battlecry ( Draw ( ) , Player #MERGE# Selector ( ) ) , Battlecry ( Transform ( Gnomish #MERGE# Chicken ( ) ) , Last #MERGE# Drawn #MERGE# Selector ( ) , Matches ( Last #MERGE# Drawn #MERGE# Selector ( ) , Is #MERGE# Minion ( ) ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 2 )
class Iron #MERGE# Juggernaut ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Iron Juggernaut " , 6 , CHARACTER_CLASS . WARRIOR , CARD_RARITY . LEGENDARY , minion_type = MINION_TYPE . MECH , battlecry = Battlecry ( Add #MERGE# Card ( Burrowing #MERGE# Mine ( ) , add_to_deck = True ) , Player #MERGE# Selector ( Enemy #MERGE# Player ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 6 , 5 )
class Madder #MERGE# Bomber ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Madder Bomber " , 5 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE , battlecry = Battlecry ( Damage ( 1 ) , Character #MERGE# Selector ( players = Both #MERGE# Player ( ) , picker = Random #MERGE# Picker ( 6 ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 5 , 4 )
class Mini #MERGE# Mage ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Mini-Mage " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . EPIC ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 1 , stealth = True , spell_damage = 1 )
class Piloted #MERGE# Sky #MERGE# Golem ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Piloted Sky Golem " , 6 , CHARACTER_CLASS . ALL , CARD_RARITY . EPIC , minion_type = MINION_TYPE . MECH ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 6 , 4 , deathrattle = Deathrattle ( Summon ( Card #MERGE# Query ( conditions = [ Mana #MERGE# Cost ( 4 ) , Is #MERGE# Minion ( ) ] ) ) , Player #MERGE# Selector ( ) ) )
class Scarlet #MERGE# Purifier ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Scarlet Purifier " , 3 , CHARACTER_CLASS . PALADIN , CARD_RARITY . RARE , battlecry = Battlecry ( Damage ( 2 ) , Minion #MERGE# Selector ( Minion #MERGE# Has #MERGE# Deathrattle ( ) , Both #MERGE# Player ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 3 )
class Siltfin #MERGE# Spiritwalker ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Siltfin Spiritwalker " , 4 , CHARACTER_CLASS . SHAMAN , CARD_RARITY . EPIC , minion_type = MINION_TYPE . MURLOC , overload = 1 ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 5 , effects = [ Effect ( Minion #MERGE# Died ( Is #MERGE# Type ( MINION_TYPE . MURLOC ) ) , Action #MERGE# Tag ( Draw ( ) , Player #MERGE# Selector ( ) ) ) ] )
class Tinkertown #MERGE# Technician ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# from hearthbreaker . cards . spells . neutral import spare_part_list #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Tinkertown Technician " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , battlecry = ( Battlecry ( Give ( [ Buff ( Change #MERGE# Attack ( 1 ) ) , Buff ( Change #MERGE# Health ( 1 ) ) ] ) , Self #MERGE# Selector ( ) , Greater #MERGE# Than ( Count ( Minion #MERGE# Selector ( Is #MERGE# Type ( MINION_TYPE . MECH ) ) ) , value = 0 ) ) , Battlecry ( Add #MERGE# Card ( Card #MERGE# Query ( source = CARD_SOURCE . LIST , source_list = spare_part_list ) ) , Player #MERGE# Selector ( ) , Greater #MERGE# Than ( Count ( Minion #MERGE# Selector ( Is #MERGE# Type ( MINION_TYPE . MECH ) ) ) , value = 0 ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 3 )
class Voljin ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Vol'jin " , 5 , CHARACTER_CLASS . PRIEST , CARD_RARITY . LEGENDARY , battlecry = Battlecry ( Swap #MERGE# Stats ( " health " , " health " , True ) , Minion #MERGE# Selector ( players = Both #MERGE# Player ( ) , picker = User #MERGE# Picker ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 6 , 2 )
class Deaths #MERGE# Bite ( Weapon #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Death's Bite " , 4 , CHARACTER_CLASS . WARRIOR , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_weapon ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Weapon ( 4 , 2 , deathrattle = Deathrattle ( Damage ( 1 ) , Minion #MERGE# Selector ( players = Both #MERGE# Player ( ) ) ) )
class Maexxna ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Maexxna " , 6 , CHARACTER_CLASS . ALL , CARD_RARITY . LEGENDARY , minion_type = MINION_TYPE . BEAST ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 8 , effects = [ Effect ( Did #MERGE# Damage ( ) , Action #MERGE# Tag ( Kill ( ) , Target #MERGE# Selector ( Is #MERGE# Minion ( ) ) ) ) ] )
class Sludge #MERGE# Belcher ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Sludge Belcher " , 5 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 5 , taunt = True , deathrattle = Deathrattle ( Summon ( Slime ( ) ) , Player #MERGE# Selector ( ) ) )
class Voidcaller ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Voidcaller " , 4 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . COMMON , minion_type = MINION_TYPE . DEMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 4 , deathrattle = Deathrattle ( Summon ( Card #MERGE# Query ( conditions = [ Is #MERGE# Type ( MINION_TYPE . DEMON ) ] , source = CARD_SOURCE . MY_HAND ) ) , Player #MERGE# Selector ( ) ) )
class Alexstrasza ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Alexstrasza " , 9 , CHARACTER_CLASS . ALL , CARD_RARITY . LEGENDARY , minion_type = MINION_TYPE . DRAGON , battlecry = Battlecry ( Set #MERGE# Health ( 15 ) , Hero #MERGE# Selector ( players = Both #MERGE# Player ( ) , picker = User #MERGE# Picker ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 8 , 8 )
class Arcane #MERGE# Golem ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Arcane Golem " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE , battlecry = Battlecry ( Give #MERGE# Mana #MERGE# Crystal ( ) , Player #MERGE# Selector ( players = Enemy #MERGE# Player ( ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 2 , charge = True )
class Baine #MERGE# Bloodhoof ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Baine Bloodhoof " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . LEGENDARY , False ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 5 )
class Blade #MERGE# Flurry ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Blade Flurry " , 2 , CHARACTER_CLASS . ROGUE , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# if player . weapon is not None : #NEWLINE# #INDENT# #INDENT# #INDENT# attack_power = player . effective_spell_damage ( player . hero . calculate_attack ( ) ) #NEWLINE# #INDENT# #INDENT# #INDENT# player . weapon . destroy ( ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# for minion in copy . copy ( game . other_player . minions ) : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# minion . damage ( attack_power , self ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# game . other_player . hero . damage ( attack_power , self )
class Brawl ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Brawl " , 5 , CHARACTER_CLASS . WARRIOR , CARD_RARITY . EPIC ) #NEWLINE# #NEWLINE# #INDENT# def can_use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# return super ( ) . can_use ( player , game ) and len ( player . minions ) + len ( player . opponent . minions ) >= 2 #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# minions = copy . copy ( player . minions ) #NEWLINE# #INDENT# #INDENT# minions . extend ( game . other_player . minions ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# if len ( minions ) > 1 : #NEWLINE# #INDENT# #INDENT# #INDENT# survivor = game . random_choice ( minions ) #NEWLINE# #INDENT# #INDENT# #INDENT# for minion in minions : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# if minion is not survivor : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# #INDENT# minion . die ( self )
class Conceal ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Conceal " , 1 , CHARACTER_CLASS . ROGUE , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# for minion in player . minions : #NEWLINE# #INDENT# #INDENT# #INDENT# if not minion . stealth : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# minion . add_buff ( Buff #MERGE# Until ( Stealth ( ) , Turn #MERGE# Started ( ) ) )
class Defender #MERGE# Minion ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Defender " , 1 , CHARACTER_CLASS . PALADIN , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , p ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 1 )
class Doomguard ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Doomguard " , 5 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . RARE , minion_type = MINION_TYPE . DEMON , battlecry = Battlecry ( Discard ( amount = 2 ) , Player #MERGE# Selector ( ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 5 , 7 , charge = True )
class Earth #MERGE# Elemental ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Earth Elemental " , 5 , CHARACTER_CLASS . SHAMAN , CARD_RARITY . EPIC , overload = 3 ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 7 , 8 , taunt = True )
class Explosive #MERGE# Trap ( Secret #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Explosive Trap " , 2 , CHARACTER_CLASS . HUNTER , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def activate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . bind ( " character_attack " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def deactivate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . unbind ( " character_attack " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def _reveal ( self , attacker , target ) : #NEWLINE# #INDENT# #INDENT# if isinstance ( target , Hero ) : #NEWLINE# #INDENT# #INDENT# #INDENT# game = attacker . player . game #NEWLINE# #INDENT# #INDENT# #INDENT# enemies = copy . copy ( game . current_player . minions ) #NEWLINE# #INDENT# #INDENT# #INDENT# enemies . append ( game . current_player . hero ) #NEWLINE# #INDENT# #INDENT# #INDENT# for enemy in enemies : #NEWLINE# #INDENT# #INDENT# #INDENT# #INDENT# enemy . damage ( 2 , None ) #NEWLINE# #INDENT# #INDENT# #INDENT# game . check_delayed ( ) #NEWLINE# #INDENT# #INDENT# #INDENT# super ( ) . reveal ( )
class Flame #MERGE# Of #MERGE# Azzinoth ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Flame of Azzinoth " , 1 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , False ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 1 )
class Gnoll ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Gnoll " , 2 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , False ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 2 , taunt = True )
class Holy #MERGE# Wrath ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Holy Wrath " , 5 , CHARACTER_CLASS . PALADIN , CARD_RARITY . RARE , target_func = hearthbreaker . targeting . find_spell_target ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# fatigue = False #NEWLINE# #INDENT# #INDENT# if player . deck . left == 0 : #NEWLINE# #INDENT# #INDENT# #INDENT# fatigue = True #NEWLINE# #NEWLINE# #INDENT# #INDENT# player . draw ( ) #NEWLINE# #INDENT# #INDENT# if not fatigue : #NEWLINE# #INDENT# #INDENT# #INDENT# cost = player . hand [ - 1 ] . mana #NEWLINE# #INDENT# #INDENT# #INDENT# self . target . damage ( player . effective_spell_damage ( cost ) , self )
class Infernal ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Infernal " , 6 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . COMMON , False , minion_type = MINION_TYPE . DEMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 6 , 6 )
class Kirin #MERGE# Tor #MERGE# Mage ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Kirin Tor Mage " , 3 , CHARACTER_CLASS . MAGE , CARD_RARITY . RARE , battlecry = Battlecry ( Give #MERGE# Aura ( [ Aura #MERGE# Until ( Mana #MERGE# Change ( - 100 ) , Card #MERGE# Selector ( condition = Is #MERGE# Secret ( ) ) , Card #MERGE# Played ( Is #MERGE# Secret ( ) ) ) ] ) , Player #MERGE# Selector ( ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 4 , 3 )
class Lightwarden ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Lightwarden " , 1 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 1 , 2 , effects = [ Effect ( Character #MERGE# Healed ( player = Both #MERGE# Player ( ) ) , Action #MERGE# Tag ( Give ( Change #MERGE# Attack ( 2 ) ) , Self #MERGE# Selector ( ) ) ) ] )
class Mana #MERGE# Wraith ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Mana Wraith " , 2 , CHARACTER_CLASS . ALL , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 2 , auras = [ Aura ( Mana #MERGE# Change ( 1 ) , Card #MERGE# Selector ( Both #MERGE# Player ( ) , Is #MERGE# Minion ( ) ) ) ] )
class Misdirection ( Secret #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Misdirection " , 2 , CHARACTER_CLASS . HUNTER , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def activate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . bind ( " character_attack " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def deactivate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . unbind ( " character_attack " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def _reveal ( self , character , target ) : #NEWLINE# #INDENT# #INDENT# if isinstance ( target , Hero ) and not character . removed : #NEWLINE# #INDENT# #INDENT# #INDENT# game = character . player . game #NEWLINE# #INDENT# #INDENT# #INDENT# possibilities = copy . copy ( game . current_player . minions ) #NEWLINE# #INDENT# #INDENT# #INDENT# possibilities . extend ( game . other_player . minions ) #NEWLINE# #INDENT# #INDENT# #INDENT# possibilities . append ( game . current_player . hero ) #NEWLINE# #INDENT# #INDENT# #INDENT# possibilities . append ( game . other_player . hero ) #NEWLINE# #INDENT# #INDENT# #INDENT# possibilities . remove ( character . current_target ) #NEWLINE# #INDENT# #INDENT# #INDENT# character . current_target = game . random_choice ( possibilities ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# super ( ) . reveal ( )
class Noble #MERGE# Sacrifice ( Secret #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Noble Sacrifice " , 1 , CHARACTER_CLASS . PALADIN , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def _reveal ( self , attacker , target ) : #NEWLINE# #INDENT# #INDENT# player = attacker . player . game . other_player #NEWLINE# #INDENT# #INDENT# if len ( player . minions ) < 7 and not attacker . removed : #NEWLINE# #INDENT# #INDENT# #INDENT# from hearthbreaker . cards . minions . paladin import Defender #MERGE# Minion #NEWLINE# #INDENT# #INDENT# #INDENT# defender = Defender #MERGE# Minion ( ) #NEWLINE# #INDENT# #INDENT# #INDENT# defender . summon ( player , player . game , len ( player . minions ) ) #NEWLINE# #INDENT# #INDENT# #INDENT# attacker . current_target = player . minions [ - 1 ] #NEWLINE# #INDENT# #INDENT# #INDENT# super ( ) . reveal ( ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# def activate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . bind ( " character_attack " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def deactivate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . opponent . unbind ( " character_attack " , self . _reveal )
class Power #MERGE# Of #MERGE# The #MERGE# Wild ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Power of the Wild " , 2 , CHARACTER_CLASS . DRUID , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# option = player . agent . choose_option ( [ Leader #MERGE# Of #MERGE# The #MERGE# Pack ( ) , Summon #MERGE# Panther ( ) ] , player ) #NEWLINE# #INDENT# #INDENT# option . use ( player , game )
class Redemption ( Secret #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Redemption " , 1 , CHARACTER_CLASS . PALADIN , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def _reveal ( self , minion , by ) : #NEWLINE# #INDENT# #INDENT# resurrection = minion . card . summon ( minion . player , minion . game , min ( minion . index , len ( minion . player . minions ) ) ) #NEWLINE# #INDENT# #INDENT# if resurrection : #NEWLINE# #INDENT# #INDENT# #INDENT# resurrection . health = 1 #NEWLINE# #INDENT# #INDENT# #INDENT# super ( ) . reveal ( ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# def activate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . bind ( " minion_died " , self . _reveal ) #NEWLINE# #NEWLINE# #INDENT# def deactivate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . unbind ( " minion_died " , self . _reveal )
class Shadow #MERGE# Madness ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Shadow Madness " , 4 , CHARACTER_CLASS . PRIEST , CARD_RARITY . RARE , target_func = hearthbreaker . targeting . find_enemy_minion_spell_target , filter_func = lambda target : target . calculate_attack ( ) <= 3 and target . spell_targetable ( ) ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# minion = self . target . copy ( player ) #NEWLINE# #INDENT# #INDENT# minion . active = True #NEWLINE# #INDENT# #INDENT# minion . exhausted = False #NEWLINE# #NEWLINE# #INDENT# #INDENT# self . target . remove_from_board ( ) #NEWLINE# #INDENT# #INDENT# minion . add_to_board ( len ( player . minions ) ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# minion . add_buff ( Buff #MERGE# Until ( Stolen ( ) , Turn #MERGE# Ended ( ) ) ) #NEWLINE# #NEWLINE# #INDENT# def can_use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# return super ( ) . can_use ( player , game ) and len ( player . minions ) < 7
class Siphon #MERGE# Soul ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Siphon Soul " , 6 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . RARE , target_func = hearthbreaker . targeting . find_minion_spell_target ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# self . target . die ( self ) #NEWLINE# #INDENT# #INDENT# player . hero . heal ( player . effective_heal_power ( 3 ) , self )
class Spellbender ( Secret #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Spellbender " , 3 , CHARACTER_CLASS . MAGE , CARD_RARITY . EPIC ) #NEWLINE# #INDENT# #INDENT# self . player = None #NEWLINE# #NEWLINE# #INDENT# def _reveal ( self , card , index ) : #NEWLINE# #INDENT# #INDENT# if card . is_spell ( ) and len ( self . player . minions ) < 7 and card . target and card . target . is_minion ( ) : #NEWLINE# #INDENT# #INDENT# #INDENT# Spellbender #MERGE# Minion ( ) . summon ( self . player , self . player . game , len ( self . player . minions ) ) #NEWLINE# #INDENT# #INDENT# #INDENT# card . target = self . player . minions [ - 1 ] #NEWLINE# #INDENT# #INDENT# #INDENT# super ( ) . reveal ( ) #NEWLINE# #NEWLINE# #INDENT# #INDENT# #INDENT# def activate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . game . current_player . bind ( " card_played " , self . _reveal ) #NEWLINE# #INDENT# #INDENT# self . player = player #NEWLINE# #NEWLINE# #INDENT# def deactivate ( self , player ) : #NEWLINE# #INDENT# #INDENT# player . game . current_player . unbind ( " card_played " , self . _reveal ) #NEWLINE# #INDENT# #INDENT# self . player = None
class Summoning #MERGE# Portal ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Summoning Portal " , 4 , CHARACTER_CLASS . WARLOCK , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 0 , 4 , auras = [ Aura ( Mana #MERGE# Change ( - 2 , 1 , minimum = 1 ) , Card #MERGE# Selector ( condition = Is #MERGE# Minion ( ) ) ) ] )
class Thrallmar #MERGE# Farseer ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Thrallmar Farseer " , 3 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 3 , windfury = True )
class Upgrade ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Upgrade! " , 1 , CHARACTER_CLASS . WARRIOR , CARD_RARITY . RARE ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# from hearthbreaker . cards . weapons . warrior import Heavy #MERGE# Axe #NEWLINE# #INDENT# #INDENT# if player . weapon : #NEWLINE# #INDENT# #INDENT# #INDENT# player . weapon . durability += 1 #NEWLINE# #INDENT# #INDENT# #INDENT# player . weapon . base_attack += 1 #NEWLINE# #INDENT# #INDENT# else : #NEWLINE# #INDENT# #INDENT# #INDENT# heavy_axe = Heavy #MERGE# Axe ( ) . create_weapon ( player ) #NEWLINE# #INDENT# #INDENT# #INDENT# heavy_axe . equip ( player )
class Wisp ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Wisp " , 0 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 1 , 1 )
class Black #MERGE# Whelp ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Black Whelp " , 1 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , False , MINION_TYPE . DRAGON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 2 , 1 )
class Dragonkin #MERGE# Sorcerer ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Dragonkin Sorcerer " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , minion_type = MINION_TYPE . DRAGON ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 3 , 5 , effects = [ Effect ( Spell #MERGE# Targeted ( ) , [ Action #MERGE# Tag ( Give ( [ Buff ( Change #MERGE# Attack ( 1 ) ) , Buff ( Change #MERGE# Health ( 1 ) ) ] ) , Self #MERGE# Selector ( ) ) ] ) ] )
class Hungry #MERGE# Dragon ( Minion #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Hungry Dragon " , 4 , CHARACTER_CLASS . ALL , CARD_RARITY . COMMON , minion_type = MINION_TYPE . DRAGON , battlecry = ( Battlecry ( Summon ( Card #MERGE# Query ( conditions = [ Mana #MERGE# Cost ( 1 ) , Is #MERGE# Minion ( ) ] ) ) , Player #MERGE# Selector ( Enemy #MERGE# Player ( ) ) ) ) ) #NEWLINE# #NEWLINE# #INDENT# def create_minion ( self , player ) : #NEWLINE# #INDENT# #INDENT# return Minion ( 5 , 6 )
class Solemn #MERGE# Vigil ( Spell #MERGE# Card ) : #NEWLINE# #INDENT# def __init__ ( self ) : #NEWLINE# #INDENT# #INDENT# super ( ) . __init__ ( " Solemn Vigil " , 5 , CHARACTER_CLASS . PALADIN , CARD_RARITY . COMMON , buffs = [ Buff ( Mana #MERGE# Change ( Count ( Dead #MERGE# Minion #MERGE# Selector ( players = Both #MERGE# Player ( ) ) ) , - 1 ) ) ] ) #NEWLINE# #NEWLINE# #INDENT# def use ( self , player , game ) : #NEWLINE# #INDENT# #INDENT# super ( ) . use ( player , game ) #NEWLINE# #INDENT# #INDENT# for n in range ( 0 , 2 ) : #NEWLINE# #INDENT# #INDENT# #INDENT# player . draw ( )
